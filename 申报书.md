# moonbit-roaring 项目申报书

## 基本信息
- 项目名称：moonbit-roaring：面向 MoonBit 的 RoaringBitmap 压缩位图库
- 参赛者：xcc-ordinary
- 联系方式：（请补充）
- GitHub 仓库链接：https://github.com/xcc-ordinary/moonbit-roaring
- 项目方向：MoonBit 数据结构 / 压缩位图基础库
- 是否为原创项目：否，参考已有开源项目（CRoaring / RoaringFormatSpec）的算法与序列化格式，代码为独立编写

## 项目简介
moonbit-roaring 是 RoaringBitmap 压缩位图在 MoonBit 上的实现。RoaringBitmap 把整数集合按高 16 位分桶，每个桶根据数据分布自动选择数组、位图或连续区间（Run）三种容器中最省字节的一种存储，因此既能表示稀疏集合也能表示稠密集合，并直接在压缩数据上做并集、交集、差集等运算。这类结构在 Lucene、ClickHouse、Druid、Spark 等系统里用来做倒排索引和过滤条件的位图运算。本项目把 Run 容器优化真正接入 add/union/intersect 等主路径，并实现了与 CRoaring 二进制兼容的官方 portable 序列化格式，用 roaring-wasm 生成的真实数据做了字节级比对测试。

## 项目方向与通用性说明
项目定位是通用的整数集合数据结构库，不绑定具体业务场景。任何需要对大量整数 ID（文档 ID、用户 ID、时间戳分桶、事件 ID 等）做存储、去重、集合运算或跨语言交换的 MoonBit 程序都可以直接依赖这个包，属于基础设施层，不依赖特定框架或运行时环境。

## 预期使用场景
1. **搜索引擎倒排索引**：每个关键词对应一个文档 ID 的位图，查询时用 `intersect`/`union` 做多关键词的 AND/OR 检索，`union_all` 一次合并多个词的 posting list。
2. **时间序列/日志过滤**：某个时间窗口内的事件 ID 是连续区间时，`from_range` 直接生成 Run 容器（几十字节），不用像稠密位图那样占 8KB，再用 `serialize` 把结果发给下游分析管道。
3. **跨语言数据交换**：MoonBit 服务序列化一批活跃用户 ID，Java/Go/Rust/Python 端的 RoaringBitmap 实现可以直接反序列化，不需要额外写转换协议。
4. **排名与分页查询**：用 `rank`/`select` 在不完全展开位图的情况下回答"某个值排第几"“第 N 小的值是什么”，适合做游标分页或统计。

## 拟实现的核心功能
- 三种容器（Array/Bitmap/Run）及基于字节成本的自动转换，转换逻辑真正挂在 `add`/`add_many`/`remove`/`union`/`intersect`/`difference`/`xor` 上
- `add_range`/`remove_range`/`contains_range`/`from_range`：整桶命中区间时 O(1) 折叠为单个 Run 容器
- `serialize(portable)`/`deserialize`：与 CRoaring 官方 portable 格式二进制兼容，并对截断数据做边界检查
- `iter()` 惰性迭代、`rank`/`select`
- `union_all`/`intersect_all` 多路合并
- 基于 roaring-wasm 生成数据的 golden fixture 测试，以及边界/tie-break 用例，共 120+ 个测试

## 参考项目说明
- 参考项目名称：CRoaring（RoaringBitmap 的 C 参考实现）及 RoaringFormatSpec
- 参考项目链接：https://github.com/RoaringBitmap/CRoaring 、 https://github.com/RoaringBitmap/RoaringFormatSpec
- 参考项目许可证：Apache-2.0
- 本项目许可证：Apache-2.0

本项目没有移植 CRoaring 的 C 代码，而是按照 RoaringBitmap 的容器选择算法和官方序列化格式规范，用 MoonBit 从零实现，并以 CRoaring/roaring-wasm 生成的真实字节流作为正确性的验证基准。
